import { memo } from 'react';
import { Box, Typography } from '@mui/material';

const AssistantPanel = memo(({ documentId, content }) => {
  return (
    <Box>
      <Typography>
        AI Assistant Panel (Coming Soon)
      </Typography>
    </Box>
  );
});

export default AssistantPanel; 