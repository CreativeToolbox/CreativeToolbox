import { Paper, Typography } from '@mui/material';

export default function CharactersPanel({ documentId }) {
  return (
    <Paper sx={{ p: 2 }}>
      <Typography variant="h6">Characters Panel</Typography>
      <Typography color="text.secondary">Coming soon...</Typography>
    </Paper>
  );
} 