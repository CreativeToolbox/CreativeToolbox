import { Paper, Typography } from '@mui/material';

export default function ThemesPanel({ documentId }) {
  return (
    <Paper sx={{ p: 2 }}>
      <Typography variant="h6">Themes Panel</Typography>
      <Typography color="text.secondary">Coming soon...</Typography>
    </Paper>
  );
} 